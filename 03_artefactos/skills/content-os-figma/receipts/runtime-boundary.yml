schema_version: 1
receipt_id: COS-FGM-RUNTIME-001
skill_id: content-os-figma
version: 0.1.0
runtime_scope: local-evaluation-only
toolchain_dependencies:
  playwright: 1.61.1
  gsap: 3.15.0
  ffmpeg: libx264
figma_access: read-only-rest-at-import-time
render_adapter: skills/content-os-core/scripts/render-html.ts
network_in_render_path: false
remote_media_fail_mode: closed
forbidden_apis:
  - Math.random
  - Date.now
  - new Date
  - fetch
  - setTimeout
  - setInterval
production_runtime_status: local_capability_only
publication_authority: false
notes:
  - No new runtime dependency added (Playwright + GSAP + FFmpeg already toolchain-pinned).
  - Figma I/O is import-time only; render sees local frozen files (no network).
  - No Figma URL is left in the composition — assets freeze under assets/figma/ with provenance.
  - Brand variables resolve as CSS custom properties; var(--slug, literal) re-brands without re-import.
