# Positive fixture: valid stage ground + seam assembly
composition_id: smc-demo-001
stage_ground:
  opaque: true
  background: 'var(--canvas-deep, var(--canvas, #000))'
seams:
  - from: '#el-scene-01'
    to: '#el-scene-02'
    duration_s: 0.6
    track_index: 0
    hold_final_frame: true
  - from: '#el-scene-02'
    to: '#el-scene-03'
    duration_s: 0.4
    track_index: 1
    hold_final_frame: true
# contract tokens present: data-composition-id, data-duration, data-start,
# window.__timelines, gsap.timeline paused: true, RENDERED_DRAFT
# opaque stage ground, white-flash guard, ping-pong track 0/1, hold final frame
