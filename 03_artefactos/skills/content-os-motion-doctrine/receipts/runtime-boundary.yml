schema_version: 1
receipt_id: COS-MDR-RUNTIME-001
skill_id: content-os-motion-doctrine
version: 0.1.0
runtime_scope: local-evaluation-only
toolchain_dependencies:
  playwright: 1.61.1
  gsap: 3.15.0
  ffmpeg: libx264
render_adapter: skills/content-os-core/scripts/render-html.ts
network_in_render_path: false
remote_media_fail_mode: closed
forbidden_apis:
  - Math.random
  - Date.now
  - new Date
  - fetch
  - setTimeout
  - setInterval
production_runtime_status: local_capability_only
publication_authority: false
notes:
  - No new runtime dependency added (Playwright + GSAP + FFmpeg already toolchain-pinned).
  - Gateway doctrine; routes to content-os-cut-the-curve + content-os-seam-craft.
  - Vector ledger (ledger.json) authored before master timeline; seam-gate verifies.
  - No idle wobble; sustained-motion route owns every phase between entry and exit.
