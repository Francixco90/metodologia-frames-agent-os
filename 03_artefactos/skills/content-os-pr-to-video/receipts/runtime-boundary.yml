receipt_id: RCP-H03-CONTENT-OS-PR-TO-VIDEO-001
schema_version: runtime-boundary-v1
skill_id: content-os-pr-to-video
issued: 2026-08-03
actor: RT-07-H03-PRODUCER
scope:
  - github pr to code-change explainer video orchestration
  - 8-step gated workflow
  - capability delegation on-demand
  - code-editorial fixed style
runtime_boundary:
  allowed_scoped:
    - static_workflow_state_parsing
    - step_gate_validation
    - capability_map_declaration
    - deterministic_orchestration
    - gh_pr_ingest_step1_only
  out_of_scope:
    - cli_install
    - remote_media_default
    - html_to_mp4_render_direct
    - web_capture
    - footage
    - external_assets
    - google_fonts_cdn
    - network_in_render_path
runtime_rules:
  - orchestrator_no_rules
  - code_change_input_no_capture
  - step_gated
  - delegate_capabilities_on_demand
  - render_path_offline_first
  - deterministic
  - seek_safe
  - rendered_draft_not_human_approved
notes: >
  Workflow orquesta steps + gates. Design y motion viven en Fase 2 capabilities.
  HTML-to-MP4 render via content-os-core Playwright adapter. Media via
  content-os-media offline cascade (remoto opt-in auth-gated). Input es un PR
  (code change via gh en Step 1, único network step, read-only, deterministic
  dado un PR ref). No web capture, no footage. Style siempre code-editorial.
  Compositions (Step 5-6) offline/deterministic. No network en render path.
