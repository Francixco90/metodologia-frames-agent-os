schema_version: 1
receipt_id: COS-RMP-RUNTIME-001
skill_id: content-os-remotion-maps
version: 0.1.0
runtime_scope: local-evaluation-only
toolchain_dependencies:
  remotion: 4.0.494
  react: 19.2.7
  ffmpeg: libx264
render_adapter: renderers/remotion/src/Root.tsx
network_in_render_path: false
remote_media_fail_mode: closed
forbidden_apis:
  - Math.random
  - Date.now
  - new Date
  - fetch
  - setTimeout
  - setInterval
production_runtime_status: local_capability_only
publication_authority: false
notes:
  - No new runtime dependency added (Remotion + React + FFmpeg already toolchain-pinned).
  - Mapbox key (if used) opt-in auth-gated fail-closed; prefer pre-rendered snapshot for determinism.
  - Animation clock = useCurrentFrame(); never CSS transition/animation.
  - License verdict: see receipts/dependency-audits/H03-LIC-REMOTION-001.yml (source-available-publisher addendum).
  - Production gates G13-G17 manual; render stays RENDERED_DRAFT.
