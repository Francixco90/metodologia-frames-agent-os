schema_version: 1
receipt_id: COS-COV-RUNTIME-001
skill_id: content-os-captions-overlay
version: 0.1.0
runtime_scope: local-evaluation-only
toolchain_dependencies:
  playwright: 1.61.1
  gsap: 3.15.0
  ffmpeg: libx264
render_adapter: skills/content-os-core/scripts/render-html.ts
network_in_render_path: false
remote_media_fail_mode: closed
forbidden_apis:
  - Math.random
  - Date.now
  - new Date
  - fetch
  - setTimeout
  - setInterval
production_runtime_status: local_capability_only
publication_authority: false
notes:
  - No new runtime dependency added (Playwright + GSAP + FFmpeg already toolchain-pinned).
  - Applies ON TOP of content-os-embedded-captions; this skill is doctrine, not a pipeline.
  - Captions composited as overlay ON TOP of film; no reserved bottom band.
  - Composition centered on true vertical center y = H/2; no content shift for captions.
  - No machine keep-out gate; caption-over-content legibility judged visually.
