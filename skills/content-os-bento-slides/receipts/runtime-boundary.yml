schema_version: 1
receipt_id: COS-BNS-RUNTIME-001
skill_id: content-os-bento-slides
version: 0.1.0
runtime_scope: local-evaluation-only
toolchain_dependencies:
  playwright: 1.61.1
render_adapter: content-os-core/html-composition
network_in_render_path: false
authoring_time_network: true
authoring_time_network_scope: bento-app-release-and-agent-schema-fetch-only
remote_media_fail_mode: closed
forbidden_apis:
  - Math.random
  - Date.now
  - new Date
  - fetch
  - setTimeout
  - setInterval
production_runtime_status: local_capability_only
publication_authority: false
notes:
  - No new runtime dependency added (Playwright already toolchain-pinned for HTML export).
  - .bento.html document JSON is deterministic given content inputs.
  - Fetching the bento app release + agent schema is authoring-time, not document-time.
  - Assets embedded as data URIs in doc.assets; the file stays self-contained.
  - MIT vendor reference — no relicensing of vendor source.
  - Production gates G13-G17 manual; deck/screenshot stays RENDERED_DRAFT.
