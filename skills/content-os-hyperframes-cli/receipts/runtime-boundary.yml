schema_version: 1
receipt_id: COS-HFC-RUNTIME-001
skill_id: content-os-hyperframes-cli
version: 0.1.0
runtime_scope: local-evaluation-only
toolchain_dependencies:
  playwright: 1.61.1
  gsap: 3.15.0
  ffmpeg: libx264
  node: '22'
render_adapter: skills/content-os-core/scripts/render-html.ts
network_in_render_path: false
remote_media_fail_mode: closed
forbidden_apis:
  - Math.random
  - Date.now
  - new Date
  - fetch
  - setTimeout
  - setInterval
production_runtime_status: local_capability_only
publication_authority: false
notes:
  - No new runtime dependency added (Playwright + GSAP + FFmpeg already toolchain-pinned).
  - CLI loop is doctrine only; render happens only after explicit approval at preview.
  - doctor --json exits zero always; gate on its payload (.ok), not the exit code.
  - JSON paths redact the home directory as $HOME; redaction is not reversed.
