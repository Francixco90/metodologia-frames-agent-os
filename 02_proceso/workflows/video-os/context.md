# Contexto: Video OS

## Activación

R6 seleccionó un video o derivado audiovisual. Leer `index.ts` y el estado de la tarea. [CONFIG]

## Carga JIT

- V00: intake + `archetypes.json`.
- V01: fuentes, hashes, derechos y `regressions.json`.
- V02: spec + storyboard + `document-sections.json`.
- V03: spec aprobada y plan de General Video.
- V04: receipts, muestras y decisión vigente.

Máximo 1.800 tokens estimados por cápsula. Datos privados solo después del route lock y fuera del
repo. [CONFIG]

## Autoridad

`Spec → Compile → Verify → Review → Promote`; General Video ejecuta, Video OS orquesta.
